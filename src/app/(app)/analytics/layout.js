export const metadata = {
  title: "Analytics",
  description: "See your consistency heatmap, day score trends, study time breakdown and insights.",
};

export default function AnalyticsLayout({ children }) {
  return children;
}
