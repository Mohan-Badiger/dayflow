export const metadata = {
  title: "Goals & Targets",
  description: "Track your short-term and long-term goals. Break them down into subtasks and follow your growth progress.",
};

export default function GoalsLayout({ children }) {
  return children;
}
