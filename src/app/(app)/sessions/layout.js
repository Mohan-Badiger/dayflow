export const metadata = {
  title: "Study & Work Sessions",
  description: "Log your deep study sessions, measure learning duration, and evaluate focus sessions in real-time.",
};

export default function SessionsLayout({ children }) {
  return children;
}
