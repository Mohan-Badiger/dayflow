export const metadata = {
  title: "Diet & Nutrition",
  description: "Track your meals, hydration levels, and clean food goals. Fuel your day with optimized nutrition plans.",
};

export default function DietLayout({ children }) {
  return children;
}
