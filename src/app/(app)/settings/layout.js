export const metadata = {
  title: "Settings",
  description: "Customize your timezone, daily target hours, hydration limits, profile name, and account parameters.",
};

export default function SettingsLayout({ children }) {
  return children;
}
