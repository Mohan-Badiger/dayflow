export const metadata = {
  title: "Login",
  description: "Sign in to DayFlow to track your habits, log study sessions, plan your day, and achieve your daily targets.",
};

export default function LoginLayout({ children }) {
  return children;
}
